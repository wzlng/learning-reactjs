// project imports
import AuthWrapper from './AuthWrapper';

// ===============================|| AUTH - REGISTER ||=============================== //

function Register() {
  return (
    <AuthWrapper>
      <p>Register</p>
    </AuthWrapper>
  );
}

export default Register;
