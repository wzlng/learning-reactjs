// project imports
import AuthWrapper from './AuthWrapper';

// ===============================|| AUTH - RESET PASSWORD||=============================== //

function ResetPassword() {
  return (
    <AuthWrapper>
      <p>Reset Password</p>
    </AuthWrapper>
  );
}

export default ResetPassword;
